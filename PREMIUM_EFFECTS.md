# پایهٔ عناصر پریمیوم

این پروژه اکنون یک لایهٔ مستقل با نام‌های `fx-*` دارد. این لایه از بررسی فایل `fullstack-effects-kit.html` استخراج شده است، اما با Webflow و CSS خام آن وابستگی ندارد. فایل CSS ارسالی عمدتاً شامل reset و runtime مخصوص Webflow است؛ بنابراین فقط الگوهای بصری و تعاملی مفید آن به پایهٔ مستقل تبدیل شده‌اند.

| کلاس یا مؤلفه | کاربرد پیشنهادی | رفتار |
|---|---|---|
| `fx-reveal` | تیتر، تصویر یا کارت مهم | ورود با opacity و حرکت رو به بالا |
| `fx-marker` | یک جملهٔ کلیدی | پوشش و خروج marker روی متن |
| `fx-swap-button` | CTAهای اصلی | جابه‌جایی کاراکتری متن در hover |
| `fx-marquee` | نوار معرفی یا فهرست توانمندی | حرکت پیوسته با توقف در hover |
| `fx-award-row` | ردیف پروژه، سرویس یا لینک | رشد پس‌زمینه از پایین در hover |
| `fx-faq` | پرسش‌های متداول | باز و بسته‌شدن با height روان و آیکن چرخان |
| `fx-arrow-link` | لینک ثانویه | underline رشدکننده و حرکت فلش |

برای افزودن یک عنصر جدید، ابتدا در `client/src/index.css` از خانوادهٔ `fx-*` استفاده کنید. برای نمونه‌های React و منطق متن split، از `PremiumEffectsShelf.tsx` و `premiumEffects.ts` استفاده کنید. افکت‌های سنگین‌تر مانند arc marquee، sticky stack، شمارنده و cursor scramble در نقشهٔ پایه حفظ شده‌اند، اما فقط زمانی باید فعال شوند که واقعاً با هدف آن بخش مرتبط باشند.
