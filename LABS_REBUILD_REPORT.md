# گزارش نمونهٔ مستقل Fieldworks

نمونهٔ مستقل در مسیر `/labs-study` قرار دارد. مرجع مشاهده‌شده، [Labs گوگل](https://labs.google/) است؛ ساختار آن شامل Hero carousel، مجموعهٔ تجربه‌ها، معرفی، timeline تکامل محصول، بخش ارتباط و footer بوده است.

| لایهٔ مرجع | بازسازی مستقل Fieldworks |
|---|---|
| Hero رسانه‌ای با slider | Hero تیره با چهار تجربهٔ مستقل، کلاژ CSS، autoplay و کنترل قبل/بعد |
| کاتالوگ تجربه‌ها | carousel کارت‌های Dayline تا Soft Stitch با کارت مرکزی و کارت‌های پیرامونی |
| بخش معرفی | روایت Fieldworks با انگیزهٔ تجربه‌های کوچک و کاربردی |
| مسیر رشد محصول | timeline «نام اولیه → محصول فعلی» برای چهار نمونهٔ مستقل |
| عضویت و footer | دو CTA برای دریافت خبر و عضویت در حلقهٔ آزمون |

تمام محتوا، برند، متن، تصویرسازی و کد Fieldworks مستقل هستند. سایت مرجع فقط برای مشاهدهٔ الگوی ساختار، ریتم صفحه و قرارداد تعامل استفاده شد.

## آزمون‌های تکمیل‌شده

مسیر دسکتاپ و موبایل بررسی شد. Hero carousel در تعامل زنده تجربهٔ فعال، عنوان، توضیح، رنگ accent و progress را تغییر داد. فاصله‌های responsive در عرض 390px کنترل شدند و build پروژه با TypeScript بدون خطا تکمیل شد.

## ارجاع

[1] [Google Labs: Google's home for AI experiments](https://labs.google/)
