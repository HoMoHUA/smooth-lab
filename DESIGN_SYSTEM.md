# سیستم دیزاین Smooth Hero Lab

این سند، قرارداد استفاده از رنگ، تایپوگرافی، فاصله، مؤلفه و حرکت در Smooth Hero Lab است. هدف آن، ایجاد لندینگ‌پیجی است که هم‌زمان حس محصول کامل و یک منبع فنی قابل ارجاع برای NeXTPixel را داشته باشد.

## Foundations

| دسته | توکن | استفاده |
|---|---|---|
| رنگ | `--ds-ink`، `--ds-cobalt`، `--ds-lime`، `--ds-paper` | سلسله‌مراتب متن، CTA، تأیید و سطح زمینه |
| فاصله | `--ds-space-1` تا `--ds-space-7` | فاصله‌گذاری درون مؤلفه و بین بخش‌ها |
| شعاع | `--ds-radius-sm`، `--ds-radius-md`، `--ds-radius-lg` | کنترل میزان نرمی سطوح؛ هر سطح یک شعاع مشخص دارد |
| حرکت | `--ds-ease-out`، `--ds-ease-in-out` | ورود، hover و انتقال‌های قابل قطع |

## Component Rules

| مؤلفه | قرارداد |
|---|---|
| Button | CTA اصلی زمینهٔ Ink دارد؛ حالت hover رنگ را وارونه نمی‌کند مگر در همان صحنه دلیل روشنی داشته باشد. |
| Link | underline از یک سمت رشد می‌کند و آیکن حداکثر چهار پیکسل حرکت می‌کند. |
| Card | یک هدف، یک عنوان و یک رفتار دارد؛ کارت نباید هم‌زمان hover، marquee و parallax داشته باشد. |
| Motion Scene | هر صحنه یک observer، یک ناحیهٔ خروج و پشتیبانی از `prefers-reduced-motion` دارد. |

## Page Composition

لندینگ‌پیج از Hero، ارزش پیشنهادی، اصول طراحی، توکن‌ها، اجزای پایه، کاربردها، CTA و در انتها Effects Kit تشکیل می‌شود. Effects Kit یک ابزار فنی است و باید بعد از روایت محصولی بیاید، نه در شروع صفحه.

## Reuse

توکن‌ها در `client/src/lib/designTokens.ts` تعریف شده‌اند. نمونه‌های زنده در بخش «سیستم دیزاین» لندینگ و الگوهای پیشرفته‌تر در `ReferenceEffectsKit.tsx` نگهداری می‌شوند. هر مؤلفهٔ جدید باید پیش از اضافه‌شدن، توکن رنگ، تایپ و حرکت خودش را از همین قرارداد انتخاب کند.
