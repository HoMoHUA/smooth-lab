# نتایج آزمون زنده

## آزمون ۱: بارگذاری Hero

وضعیت: موفق. Hero شامل نشانهٔ برند، تیتر فارسی، ریزبرچسب فنی، میدان ذرات، حلقهٔ میدان و داده‌های وضعیت نمایش داده شد.

## آزمون ۲: پیوند داخلی اسکرول

وضعیت: موفق. با فعال‌کردن پیوند «اسکرول»، موقعیت native صفحه به بخش رفتار اسکرول منتقل شد. لایهٔ محتوایی با تأخیر اینرسی‌دار به موقعیت هدف همگرا شد و مقدار پیشرفت از ۰٪ به ۱۶٪ به‌روزرسانی شد.

## آزمون ۳: پیمایش لایه‌های روایی

وضعیت: موفق. دو پیمایش متوالی، بخش «میدان تعاملی» را با محتوای نمایشی، برچسب‌های داده‌ای و تصویر مدار ذرات نشان داد. مقدار پیشرفت در Hero حین آزمون به ۵۴٪ رسید که بیانگر اتصال صحیح موقعیت native و لایهٔ نمایشی نرم است.

## آزمون ۴: کنترل زندهٔ اینرسی و حالت native

تغییر کنترل اینرسی از ۰.۰۸۵ به ۰.۱۵۰ با موفقیت در Hero و پنل کنترل به‌روز شد. تغییر کلید به حالت native نیز مقدار MODE را به NATIVE تغییر داد. پس از اصلاح ارتفاع مصنوعی اسکرول، بخش کنترل‌ها بدون فضای خالی نمایش داده شد؛ آزمون بازگشت native موفق است.

## آزمون ۵: بازنشانی

تغییر اولیهٔ مقدار اینرسی و MODE موفق بود، اما بازگشت صفحه به Hero در هنگام تبدیل native به smooth پایدار نبود. علت، اجرای پیمایش native با رفتار smooth هم‌زمان با ساخت دوبارهٔ لایهٔ fixed بود. بازنشانی با `requestAnimationFrame` و پیمایش فوری اصلاح شد؛ آزمون نهایی در ادامه انجام می‌شود.

## آزمون ۶: ناوبری مستقیم کنترل‌ها

وضعیت: موفق. پس از بارگذاری مجدد پروژه، پیوند «کنترل‌ها» در Hero به‌درستی به پنل تنظیمات منتقل شد و مقدار پیش‌فرض ۰.۰۸۵ و حالت SMOOTH نمایش داده شدند.

## آزمون ۷: بازنشانی نهایی

وضعیت: موفق. با اجرای بازنشانی در حالت SMOOTH، صفحه به Hero برگشت، مقدار SCROLL به ۱٪ نزدیک شد و مقدار INERTIA روی ۰.۰۸۵ باقی ماند.

## آزمون ۸: واکنش موس در Hero

وضعیت: موفق. حرکت موس در Hero باعث ظاهرشدن نشانگر حلقوی نیمه‌شفاف و انتقال مرکز میدان نیرو شد. ذرات اطراف موقعیت نشانگر به شکل قابل مشاهده از مسیر حلقه رانده شدند.

## آزمون ۹: بارگذاری پایهٔ افکت‌ها

وضعیت: موفق. نسخهٔ جدید شامل بخش «PREMIUM EFFECTS BASE» با Reveal، Marker wipe، Text swap، Marquee، hover row و آکاردئون بارگذاری شد. پیمایش به بخش جدید با همان حلقهٔ اینرسی صفحه آغاز شد؛ بررسی تعامل آکاردئون و دکمهٔ متنی در ادامه انجام می‌شود.

## آزمون ۱۰: پایداری صفحه پس از افزوده‌شدن کتابخانه

وضعیت: موفق. پس از افزودن لایهٔ پایه، Hero و فصل رفتار اسکرول بدون شکست layout یا تداخل با میدان ذرات نمایش داده شدند. مقدار پیشرفت اسکرول نیز در حرکت صفحه به‌صورت پیوسته به‌روزرسانی شد.

## آزمون ۱۱: دسترسی به انتهای کتابخانه

پیمایش به انتهای صفحه با لایهٔ اسکرول نرم انجام شد. با توجه به اینرسی عمداً محسوس پروژه، نمایشگر در حال همگرایی به موقعیت نهایی بود؛ برای آزمون تعامل هر مؤلفه از کنترل‌های viewport پس از تثبیت استفاده می‌شود.

## آزمون ۱۲: مشاهدهٔ قفسهٔ افکت‌ها

وضعیت: موفق. قفسهٔ «المنت‌های حرکت، مرتب و آمادهٔ استفاده» در viewport نمایش داده شد. ترکیب‌بندی سرمقاله‌ای، کارت‌های Reveal/Stagger، Marker wipe و Text swap با رنگ Pulse Cobalt، بدون اختلال در ساختار موجود رندر شدند.

## آزمون ۱۳: صفحهٔ جامع ۲۳ افکت

وضعیت: موفق. دکمهٔ «۲۳ افکت» در ناوبری Hero کار کرد و به Atlas منتقل شد. Hero اختصاصی Atlas با شبکهٔ ظریف، حلقه‌های پارالاکس، ذرات، plusهای چرخان و readout `23/23` نمایش داده شد. عنوان و متن در پس‌زمینهٔ روشن خوانا باقی ماندند.

## آزمون ۱۴: فعال‌سازی ورود در صفحهٔ بلند

یک ناسازگاری در آستانهٔ IntersectionObserver شناسایی شد: ارتفاع زیاد Atlas باعث می‌شد نسبت visible آن به آستانه نرسد و نخستین مجموعهٔ کارت‌ها پنهان بماند. آستانه به مشاهدهٔ ورود واقعی viewport تغییر کرد تا Reveal و Stagger در تمام طول صفحه فعال بمانند.

## آزمون ۱۵: بازآزمون مسیر Atlas بعد از اصلاح

ناوبری مستقیم به Atlas دوباره اجرا شد. در لحظهٔ انتقال، به‌دلیل اینرسی عمدی لایهٔ اسکرول، بخش مقصد به‌صورت گذار نرم ظاهر شد؛ بررسی پس از تثبیت برای تأیید نهایی انجام می‌شود.

## آزمون ۱۶: پارالاکس واقعی برای Hero و Media

افکت‌های شمارهٔ ۱۹ و ۲۲ از حالت صرفاً تزئینی به رفتار اسکرول‌محور تبدیل شدند. حلقهٔ اصلی Hero در طول خروج Header جابه‌جایی و scale ظریف دارد؛ قاب رسانه نیز در عبور viewport از `-10%` تا `+10%` حرکت می‌کند.

## آزمون ۱۷: مجموعهٔ کامل و حالت موبایل

وضعیت: موفق. ساخت TypeScript و production build پس از تکمیل ۲۳ الگو بدون خطا انجام شد. Hero اصلی در نمای 390px خوانا و بدون برش رندر شد. قاعده‌های responsive، Atlas را به چیدمان تک‌ستونه تبدیل می‌کنند؛ progressive blur و cursor follower نیز در reduced motion یا موبایل خاموش می‌شوند. تغییر خودکار متن اسلایدر نیز در بازبینی زنده مشاهده شد.

## آزمون ۱۸: پایدارسازی متن، RTL و اسکرول

وضعیت: موفق. برچسب‌های قابل مشاهدهٔ Hero و Atlas به فارسی یکدست شدند؛ اعداد readout و شمارنده‌ها نیز با رقم فارسی نمایش داده می‌شوند. پیوندهای داخلی Atlas اکنون از موتور اسکرول پروژه استفاده می‌کنند و به‌جای anchor پیش‌فرض پرش نمی‌کنند. شمارنده‌ها فقط هنگام دیده‌شدن بخش مربوطه آغاز می‌شوند. build و TypeScript بدون خطا گذشتند؛ در بررسی کنسول و درخواست‌های شبکه نیز خطا یا پاسخ ناموفق ثبت نشد.

## آزمون ۱۹: بازسازی مرجع

پس از جایگزینی Atlas با Effects Kit بازسازی‌شده، مسیر ناوبری «۲۳ افکت» به شناسهٔ جدید `reference-kit` تغییر یافت. این اصلاح برای جلوگیری از ارسال کاربر به شناسهٔ حذف‌شدهٔ `all-effects` ضروری بود.

## آزمون ۲۰: پیمایش به Effects Kit بازسازی‌شده

دکمهٔ «۲۳ افکت» مسیر جدید را فعال کرد و اسکرول نرم به سمت محتوای پایین صفحه شروع شد. با توجه به طول زیاد صفحهٔ بازسازی‌شده، مقصد و ترازبندی بخش‌ها در مرحلهٔ تطبیق نهایی باید در چند نقطهٔ میانی نیز کنترل شود.

## آزمون ۲۱: حضور ماژول‌های افزوده

وضعیت: تأیید شد. جست‌وجوی زندهٔ DOM عبارت «Sticky Bento» را در انتهای صفحه یافت؛ بنابراین هر دو ماژول جدید در نسخهٔ اجراشده رندر شده‌اند. به علت طول بالای صفحه و لایهٔ اسکرول اینرسی‌دار، کنترل دیداری آن‌ها باید در وضعیت اسکرول بومی نیز انجام شود تا نتیجهٔ sticky و pop-out بدون مداخلهٔ transform سراسری سنجیده شود.

## آزمون ۲۲: Sticky Bento

وضعیت: موفق. بازکردن مستقیم شناسهٔ `sticky-bento` به ابتدای ماژول رسید. زمینهٔ گرادیانی، عنوان بزرگ، برچسب اثر افزوده و چوب‌های لایه‌ای رندر شدند. شناسهٔ مستقیم نشان می‌دهد بخش در ارتفاع قابل پیمایش صفحه قرار گرفته و مسیر اسکرول پس از اصلاح ارتفاع، این ماژول را دربر می‌گیرد.

## آزمون ۲۳: Pop-out Image و Explode

وضعیت: موفق. بازکردن شناسهٔ `pop-out-image` ماژول را با زمینهٔ تیره و کنترل مستقل نمایش داد. کنترل «حالت Explode» اجرا شد و متن آن به «بازگشت لایه‌ها» تغییر کرد؛ بنابراین state جداسازی دو لایهٔ تصویر فعال است.

## آزمون ۲۴: شمارنده و اسلایدر

وضعیت: موفق. بخش Utilities رندر شد؛ شمارنده‌ها از صفر آغاز کردند و در پایان به ۷۲، ۹۸٪ و ۸٫۲ رسیدند. اسلایدر خودکار نیز پس از آغاز شمارش در صفحه رندر شد و کنترل‌های نقطه‌ای آن در ساختار موجود است.

## آزمون ۲۵: تعویض خودکار اسلایدر

وضعیت: موفق. پس از گذشت چرخهٔ زمان‌بندی، اسلایدر از اسلاید نخست به اسلاید آبی «Systems that keep moving.» منتقل شد. این نتیجه با دورهٔ چهارثانیه‌ای تعریف‌شده برای الگوی مرجع هم‌خوان است.

## آزمون ۲۶: Marker Wipe مستقل

وضعیت: موفق. بازکردن مستقیم `rk-marker` کارت Marker را پس از ورود به viewport خودش نمایش داد. هر سه خط متن پس از پایان wipe در وضعیت کامل و خوانا باقی ماندند؛ آغاز حرکت دیگر به ورود کلی Effects Kit وابسته نیست.

## آزمون ۲۷: دارایی‌های Pop-out Image

وضعیت: موفق. با پیمایش کوتاه در ماژول Pop-out، تصویر زمینه و برش شفافِ اسکیت‌بردباز هم‌زمان در صحنه ظاهر شدند. لایهٔ بیرون‌زننده بالای قاب تصویری دیده شد و دارایی‌های بارگذاری‌شده از فضای پایدار پروژه پاسخ دادند.

## آزمون ۲۸: لندینگ و سند زنده

وضعیت: موفق. Hero در viewport دسکتاپ با نشان NeXTPixel Motion System، تیتر فارسی، ذرات، حلقهٔ نیرو، داده‌های کوچک و CTA رندر شد. مسیر ناوبری «سیستم» با موتور اسکرول مشترک حرکت را آغاز کرد و hash مقصد به `ds-overview` تغییر یافت؛ تثبیت موقعیت نهایی در ادامه کنترل می‌شود.

## آزمون ۲۹: مقصد سیستم دیزاین

وضعیت: موفق. بعد از پایان اینرسی، انتهای Hero و ابتدای بخش «یک زبان مشترک برای تصمیم، طراحی و اجرا» در یک viewport دیده شد. بنابراین ناوبری «سیستم» به آغاز سند زنده می‌رسد و پیوستگی دیداری بین لندینگ و مستندات حفظ شده است.

## آزمون ۳۰: نمای موبایل

وضعیت: موفق. Hero در نمای ۳۹۰×۸۴۴ با تیتر فارسی، متن راهنما، CTA و تصویرسازی ذرات بدون برش یا تداخل مشاهده شد. ناوبری ثانویه و readoutهای سنگین در breakpoint موبایل حذف می‌شوند و CTAها به عرض مناسب صفحه می‌رسند.

## مشاهدات

تصویر تمام‌صفحهٔ ابزار پیش‌نمایش، محتوای fixed را پنهان می‌کند؛ بنابراین برای کنترل واقعی Hero از نمای viewport استفاده شد. نتیجهٔ viewport نشان داد که Hero و دارایی‌های تولیدشده به‌درستی رندر می‌شوند.
