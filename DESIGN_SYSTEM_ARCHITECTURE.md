# معماری سیستم دیزاین Smooth Hero Lab

Smooth Hero Lab باید هم‌زمان دو نقش مشخص داشته باشد: نخست یک لندینگ‌پیج که ارزش محصول، مخاطب و کاربرد آن را روشن کند؛ دوم یک سند زنده که تیم طراحی و توسعه بتواند برای انتخاب توکن و مؤلفه به آن رجوع کند.

| لایه | مسئولیت | خروجی در محصول |
|---|---|---|
| Foundations | رنگ، تایپ، فاصله، radius، shadow و حرکت | توکن‌های CSS با پیشوند `--ds-` |
| Components | دکمه، لینک، کارت، برچسب، آکاردئون و کنترل | نمونهٔ زنده و قرارداد استفاده |
| Motion | ورود، متن، scroll، hover و پارالاکس | خانواده‌های `rk-*` با reduced motion |
| Landing | ارزش پیشنهادی، قابلیت‌ها، فرایند، کاربرد و CTA | روایت مختصر پیش از کتابخانهٔ جامع |
| Reference | همهٔ الگوهای Effects Kit و دو ماژول اسکرول | Effects Kit قابل آزمایش و لینک‌پذیر |

## ساختار صفحه

صفحه با Hero تعاملی شروع می‌شود و سپس به بخش‌های «سیستم در یک نگاه»، «اصول»، «توکن‌ها»، «مؤلفه‌های پایه»، «قابلیت‌ها و کاربردها»، و CTA می‌رسد. Effects Kit بعد از این روایت، به‌عنوان مرجع کامل و فنی قرار می‌گیرد، نه اولین تجربهٔ کاربر.

## قواعد پایداری

حرکت‌های پیوسته فقط در نواحی مورد نیاز اجرا می‌شوند و با `prefers-reduced-motion` غیرفعال می‌گردند. تمام افکت‌های ورود با observer مستقل فعال می‌شوند. اسکرول نرم باید ارتفاع track را با تغییرات DOM و اندازهٔ محتوا همگام نگه دارد و تمام پیوندهای داخلی از یک مسیر ناوبری مشترک استفاده کنند.
