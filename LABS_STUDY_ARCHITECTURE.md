# معماری نمونهٔ مستقل Labs

## نام و مرز نمونه

نمونه با نام **Fieldworks** ساخته می‌شود؛ یک فضای مستقل برای تجربه‌های هوش مصنوعی. ساختار تعامل و ریتم بصری از مشاهدهٔ مرجع الهام می‌گیرد، اما تمام نام‌ها، متن‌ها، تصویرسازی‌ها، رنگ‌ها و کدها مستقل هستند.

| بخش | هدف محصولی | قرارداد اجرایی |
|---|---|---|
| Hero Carousel | معرفی یک تجربهٔ شاخص | چهار اسلاید خودکار با دکمهٔ قبل/بعد، dot و کلاژ CSS مستقل |
| Experiments | کشف تجربه‌ها | carousel کارت‌های بزرگ در پس‌زمینهٔ روشن با شکل‌های رنگی بزرگ |
| About | توضیح ارزش محصول | متن دو ستونه و reveal کم‌حرکت |
| Beyond | نمایش مسیر محصول‌ها | ردیف «نام اولیه → نام نهایی» با CTA |
| Connect | تبدیل کاربر | دو CTA برابر برای عضویت و آزمون زودهنگام |
| Footer | خروجی‌های جانبی | گروه‌بندی پیوندها و نشان وضعیت |

## حرکت و پایداری

Slider هر پنج ثانیه پیشروی می‌کند، در hover یا focus متوقف می‌شود و با دکمه‌ها قابل کنترل است. بخش‌ها با observer مستقل وارد می‌شوند. در حالت `prefers-reduced-motion`، autoplay متوقف شده و transformهای داستانی به حالت نهایی می‌رسند. تصاویر و کلاژها با CSS gradients و قاب‌های تولیدی ساخته می‌شوند تا دارایی اختصاصی سایت مرجع استفاده نشود.

## مسیر

پیاده‌سازی در مسیر مستقل `/labs-study` انجام می‌شود تا ساختار فعلی Motion System دست‌نخورده بماند.
